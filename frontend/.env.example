# Copie para .env.local. O client id não é segredo: ele vai inlinado no bundle
# do browser. Precisa ser o MESMO valor do GOOGLE_OAUTH_CLIENT_ID do backend,
# senão o validador de "aud" recusa todo token com um 401 opaco.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
